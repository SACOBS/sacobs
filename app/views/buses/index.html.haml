%h2 Buses
- bootstrap_table headers:['Name', 'Year','Model', 'Capacity'], types: [:bordered, :striped, :condensed], html_options: {class: 'sortable'} do
  = render @buses

= link_to 'Add Bus', new_bus_path, class: 'btn btn-primary'
