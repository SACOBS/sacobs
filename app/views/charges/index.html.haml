.page-header
  %h2 Charges

= link_to 'Setup Charge', new_charge_path, class: t('buttons.primary'),icon: :plus

- bootstrap_table headers:['Description', 'Percentage', 'Actions'], types: [:bordered, :striped, :condensed], html_options: {class: 'sortable'} do
  = render @charges

= link_to 'Setup Charge', new_charge_path, class: t('buttons.primary'),icon: :plus


