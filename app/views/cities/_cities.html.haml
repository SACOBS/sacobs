= paginate cities

- bootstrap_table headers: %w(Name Venues Actions), types: [:bordered, :striped, :condensed], html_options: { class: 'sortable' } do
  = render cities, cache: true

= paginate cities