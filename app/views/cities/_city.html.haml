%tr[city]
  %td= city.name
  %td= city.venues.size
  %td= link_to 'Edit', edit_city_path(city), icon: 'icon-edit'
  %td= link_to 'Delete', city, icon: 'icon-remove', data: { confirm: 'Are you sure you want to delete this city?'}, method: :delete
