%h2 Cities

- bootstrap_table headers:['Name', 'Venues',''], types: [:bordered, :striped, :condensed], html_options: {class: 'sortable'} do
  = render @cities

= link_to 'Add City', new_city_path, class: 'btn btn-primary'
