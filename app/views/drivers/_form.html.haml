#errors= errors_for @driver
= simple_form_for @driver,  defaults: {error: false} ,html: {class: 'form-horizontal'} do |f|
  %fieldset
    = f.input :name
    = f.input :surname
  .form-actions
    = f.submit class: 'btn btn-primary'
    = link_to 'Cancel', drivers_path, class: 'btn'

