.modal.hide.fade#notes{ aria_hidden:  'true', aria_labelledby: 'notesLabel', role: 'dialog', tabindex: '-1' }
  .modal-header
    %button.close{ aria_hidden: 'true', data: { dismiss: 'modal' }, type: 'button' } ×
    %h4#notesLabel Notes
    .modal-body
      .well.well-small
        = render 'notes/form' if current_user.admin?
        %ul
          = list_of (@notes.map(&:content)) do |content|
            %li= content
