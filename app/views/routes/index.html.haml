%h2 Routes

- bootstrap_table headers:['Start City', 'End City','Cost', 'Distance','Connections', ''], types: [:bordered, :striped, :condensed], html_options: {class: 'sortable'} do
  = render @routes

= link_to 'Setup Route', route_builder_index_path(route_id: :building), method: :post