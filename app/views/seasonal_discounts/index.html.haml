.page-header
  %h2 Seasonal Discount
  %h4 These discounts will override the current passenger discount when the booking falls in between the period from and period to dates. Eg Senior Citizen will have 0% added rather than the standard 10%

= link_to 'Setup Seasonal Discount', new_seasonal_discount_path, class: t('buttons.primary'),icon: :plus
%br
%br

- bootstrap_table headers:['Passenger Type','Percentage', 'Period From', 'Period To', 'Active', 'Actions'], types: [:bordered, :striped, :condensed], html_options: {class: 'sortable'} do
  = render seasonal_discounts

= link_to 'Setup Seasonal Markup', new_seasonal_discount_path, class: t('buttons.primary'),icon: :plus


