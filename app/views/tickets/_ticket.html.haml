.details
  %p
    %strong Reference No:
    = @ticket.booking.reference_no
  %p
    %strong Today's Date:
    = l Time.zone.now, format: :long
%br
%br

.passengers
- bootstrap_table headers:['Date', 'Name' ,'From', 'Terminal', 'To' , 'Terminal', 'Departure Time', 'Arrival Time'], types: [:bordered] do
  - @ticket.passengers.each do |passenger|
    %tr
    %td= @ticket.booking.trip_start_date
    %td= passenger.full_name
    %td= @ticket.booking.from
    %td= @ticket.booking.from_venue
    %td= @ticket.booking.to
    %td= @ticket.booking.to_venue
    %td= @ticket.booking.stop.depart_time || l(Time.zone.now, format: :long)
    %td= @ticket.booking.stop.arrive_time || l(Time.zone.now, format: :long)
  - if @ticket.return
    - @ticket.passengers.each do |passenger|
      %tr
      %td= @ticket.return.trip_start_date
      %td= passenger.full_name
      %td= @ticket.return.from
      %td= @ticket.return.from_venue
      %td= @ticket.return.to
      %td= @ticket.return.to_venue
      %td= @ticket.return.stop.depart_time || l(Time.zone.now, format: :long)
      %td= @ticket.return.stop.arrive_time || l(Time.zone.now, format: :long)
%br

.costs.pull-left
  %p
  %strong Amount:
  = number_to_currency @ticket.total, unit: 'R'
  %p
  %strong Discount:
  = number_to_currency @ticket.total_discount, unit: 'R'
%br
%br
%br
.special-instructions.text-center
  %p
    %em= simple_format(settings.ticket_instructions)
%br
%br
.scripture.text-center
  %p
  %strong Thought for today
  %em= simple_format(ticket_scripture)
