%tr.nested-fields
  %td= f.object.connection.from.city.name
  %td= f.object.connection.to.city.name
  %td= f.input :depart, as: :time_picker, label: false
  %td= f.input :arrive, as: :time_picker, label: false


